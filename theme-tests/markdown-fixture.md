# Theme Test Fixture

This paragraph mixes **bold**, *italic*, and `inline code` with a [link](https://example.com/docs?mode=test).

## Lists And Notes

- First item with value `42`
- Second item with escaped punctuation: `[]{}()<>=!`
- Third item with nested phrase and ASCII-only emphasis markers

> Blockquote line one.
> Blockquote line two with `--flag` and `name=value`.

### Table

| Key | Value | Status |
| --- | ---: | :---: |
| alpha | 1 | true |
| beta | 2.5 | false |

```ts
type Pair = { left: string; right: number };
const sample: Pair = { left: "md", right: 7 };
```

1. Ordered item one
2. Ordered item two
3. Ordered item three

Final line with HTML-ish `<span data-kind="fixture">text</span>` and a horizontal rule below.

---
